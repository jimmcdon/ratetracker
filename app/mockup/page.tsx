import { CalculatorMockup } from "@/components/calculator-mockup"

export default function MockupPage() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-white to-slate-50">
      <CalculatorMockup />
    </main>
  )
} 